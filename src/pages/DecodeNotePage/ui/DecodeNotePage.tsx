import { DecodeNote } from 'features/DecodeNote';

export const DecodeNotePage = () => {
  return (
    <section id="decode">
      <div className="container">
        <DecodeNote />
      </div>
    </section>
  );
};
